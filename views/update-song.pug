extends layout

block content

  div.container
    h2 Update Song

    div.content
    form#formUpdateSong(name="updatesong",method="post",action="/updateSong/" + song.id)
        p
            input#title.form-control(type='text', name='title', value=song.title)
        p
            input#duration.form-control(type='text', name='duration', value=song.duration)
        p
            input#owner.form-control(type='text', name='owner', value=song.owner)
        p
            button.btn.btn-primary(type="submit") Update Song